[project]
name = "randgen"
version = "0.1.1"
dependencies = ["Flask==3.1.3", "gunicorn==23.0.0"]

[project.optional-dependencies]
dev = ["pytest==9.0.3"]

[tool.pytest.ini_options]
pythonpath = ["src"]